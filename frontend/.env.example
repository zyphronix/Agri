# Example environment variables for frontend
# Copy this file to `.env` and modify values for your environment.

# Base URL for backend API. If empty, frontend will use relative paths like `/api/...`.
# Example: VITE_API_URL=http://localhost:4000
VITE_API_URL=

# Optional: App display name (not required by code but useful)
VITE_APP_NAME=Crop Advisor Pro
